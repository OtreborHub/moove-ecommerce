export enum Role {
    ADMIN = "Admin",
    MEMBER = "Member",
    NONE = ""
}

export function getRole(owner: boolean, member: boolean){
    // if(owner){
    //     return Role.OWNER
    //   } else if (member) {
    //     return Role.MEMBER
    //   } else {
    //     return Role.VISITOR;
    //   }
}